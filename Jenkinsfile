pipeline {
    agent any
        stages {
            stage ('main') {
                steps {
                   script {
                   sh 'date'
                }
            }
        }
    }
}
